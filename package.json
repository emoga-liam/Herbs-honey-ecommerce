{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "artifacts/*",
    "lib/*",
    "lib/integrations/*",
    "scripts"
  ],
  "scripts": {
    "build": "yarn typecheck && yarn workspace @workspace/ffg-store build && yarn workspace @workspace/api-server build",
    "hostinger:build": "yarn build",
    "hostinger:bundle": "bash ./scripts/create-hostinger-bundle.sh",
    "hostinger:start": "node --enable-source-maps artifacts/api-server/dist/index.mjs",
    "start": "node --enable-source-maps artifacts/api-server/dist/index.mjs",
    "typecheck:libs": "tsc --build",
    "typecheck": "yarn typecheck:libs && yarn workspace @workspace/api-server typecheck && yarn workspace @workspace/ffg-store typecheck && yarn workspace @workspace/mockup-sandbox typecheck && yarn workspace @workspace/scripts typecheck"
  },
  "engines": {
    "node": ">=22 <25",
    "yarn": ">=1.22.0"
  },
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}